# Unique key for your project (must match across scans)
sonar.projectKey=my-terraform-project

# Display name in SonarQube dashboard
sonar.projectName=Terraform IaC Project

# Version (optional)
sonar.projectVersion=1.0

# Source directory 
sonar.sources=.

# Encoding
sonar.sourceEncoding=UTF-8
